apiVersion: v1
kind: Service
metadata:
  name: museum-app-nodeport
  namespace: hl08
spec:
  type: NodePort
  selector:
    app: museum-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30082  